# Database connection string for Postgres + pgvector
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/rag_db

# OpenAI API key used for embeddings and chat completion
OPENAI_API_KEY=your-openai-api-key

# Secret for signing upload tokens (set to a unique random string)
UPLOAD_SECRET=change-me-upload-secret

# Optional overrides
OCR_LANGS=eng
DOC_ID=
